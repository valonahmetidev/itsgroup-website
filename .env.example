# Server-only (Cloudflare Workers / wrangler secrets — never expose in client bundles)
ADMIN_PASSWORD=
AUTH_SECRET=

# Optional: Google Analytics 4 (public measurement ID; loads only after cookie consent)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
